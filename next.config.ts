import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  /* config options here */
  // reactCompiler: true, // Has hard dependency on the native swc binary which is blocked by Windows Defender Application Control policies. Disable to use standard WASM.
};

export default nextConfig;
